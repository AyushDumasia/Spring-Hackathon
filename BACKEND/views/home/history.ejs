<%- include("../includes/navbar.ejs") %>
<title>History</title>
</head>
<body>
    <div class="main-history-container">
        <div class="history-container">
            <% for (let historyItem of history.history) { %>
                <div class="card">
                    <h3 class="card-text"><%= historyItem.time %></h3>
                    <p class="card-title"><%= historyItem.menuItem %></p>
                    <p class="card-title">Price: ₹<%= historyItem.price %></p>
                    <p class="card-title">Date: <%= historyItem.Created_At.toLocaleDateString('en-US', { month: 'short', day: 'numeric'}) %></p>
            </div>
            <% } %>
        </div>
    </div>
    <p class="total-expensise">Total : <%=total %></p>
</body>
</html>
